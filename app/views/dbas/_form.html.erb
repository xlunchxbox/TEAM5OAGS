<%= form_for(@dba) do |f| %>
  <% if @dba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dba.errors.count, "error") %> prohibited this dba from being saved:</h2>

      <ul>
      <% @dba.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
