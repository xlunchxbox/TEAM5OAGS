<%= form_for(@cso) do |f| %>
  <% if @cso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cso.errors.count, "error") %> prohibited this cso from being saved:</h2>

      <ul>
      <% @cso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
